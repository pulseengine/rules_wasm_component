"""Toolchain configurations for the tool builder"""

package(default_visibility = ["//visibility:public"])

# This package contains macros and extensions for tool building
# No targets needed, just houses .bzl files
