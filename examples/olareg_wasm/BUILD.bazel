"""WASM-based OCI registry using olareg as inspiration - CLI WASI version"""

load("//go:defs.bzl", "go_wasm_component")

package(default_visibility = ["//visibility:public"])

# Go-based registry component using standard CLI WASI (no custom WIT)
go_wasm_component(
    name = "olareg_component",
    srcs = ["src/main.go"],
    go_mod = "go.mod",
    # Using standard CLI world instead of custom registry WIT
)
